package icons;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

public class IconTest {

    @Test
    public void testIconImagesLoaded() {
        assertNotNull(Icon.ACCELERATE, "ACCELERATE image");
        assertNotNull(Icon.BEAR, "BEAR image");
        assertNotNull(Icon.BEAR_MEAT, "BEAR_MEAT image");
        assertNotNull(Icon.CHICKEN, "CHICKEN image");
        assertNotNull(Icon.CORN, "CORN image");
        assertNotNull(Icon.CORN_SEED, "CORN_SEED image");
        assertNotNull(Icon.COW, "COW image");
        assertNotNull(Icon.DELAY, "DELAY image");
        assertNotNull(Icon.DESTROY, "DESTROY image");
        assertNotNull(Icon.EGG, "EGG image");
        assertNotNull(Icon.HORSE, "HORSE image");
        assertNotNull(Icon.HORSE_MEAT, "HORSE_MEAT image");
        assertNotNull(Icon.INSTANT_HARVEST, "INSTANT_HARVEST image");
        assertNotNull(Icon.MILK, "MILK image");
        assertNotNull(Icon.PROTECT, "PROTECT image");
        assertNotNull(Icon.PUMPKIN, "PUMPKIN image");
        assertNotNull(Icon.PUMPKIN_SEED, "PUMPKIN_SEED image");
        assertNotNull(Icon.SHARK, "SHARK image");
        assertNotNull(Icon.SHARK_FIN, "SHARK_FIN image");
        assertNotNull(Icon.SHEEP, "SHEEP image");
        assertNotNull(Icon.SHEEP_MEAT, "SHEEP_MEAT image");
        assertNotNull(Icon.STRAWBERRY, "STRAWBERRY image");
        assertNotNull(Icon.STRAWBERRY_SEED, "STRAWBERRY_SEED image");
        assertNotNull(Icon.TRAP, "TRAP image");
        assertNotNull(Icon.PLAYER1, "PLAYER1 image");
        assertNotNull(Icon.PLAYER2, "PLAYER2 image");
        assertNotNull(Icon.NOTHING, "NOTHING image");
    }
}
